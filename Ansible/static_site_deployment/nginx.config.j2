server {
    listen {{ project_http_port }};
    listen [::]:{{ project_http_port }};

    server_name {{ project_name }};

    root /var/www/{{ project_name }};
    index index.html;

    location / {
        try_files $uri $uri/ =404;
    }
}